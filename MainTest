def sum(a, b):
    return (a + b)
def split(d):
    return (d*5)

a = int(input('Enter 1st number: '))
b = int(input('Enter 2nd number: '))
d = int(input("enter a number to multuply for 5"))

print(f'Sum of {a} and {b} is {sum(a, b)} and the multiplication of {d} for 5 is {split(d)}')
